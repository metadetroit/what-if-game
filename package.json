{
  "name": "what-if-game",
  "version": "1.0.0",
  "description": "The What If? Game - A multiplayer party game for 3-15 players",
  "scripts": {
    "dev": "concurrently \"cd backend && npm run dev\" \"cd frontend && npm run dev\"",
    "build": "cd frontend && npm run build",
    "start": "cd backend && npm start",
    "start:static": "cd backend && npm run start:static",
    "install:all": "cd backend && npm install && cd ../frontend && npm install"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
